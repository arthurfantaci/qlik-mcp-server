# Qlik MCP Server Configuration
# Copy this file to .env and update with your Qlik Sense server details

# Qlik Sense Server Connection
QLIK_SERVER_URL=your-qlik-server.com
QLIK_SERVER_PORT=4747

# Qlik Sense User Authentication
# For most installations, these defaults work with certificate-based auth
QLIK_USER_DIRECTORY=INTERNAL
QLIK_USER_ID=sa_engine

# SSL Certificate Paths (relative to project root)
# You need to obtain these certificates from your Qlik Sense administrator
QLIK_CERT_ROOT=certs/root.pem
QLIK_CERT_CLIENT=certs/client.pem
QLIK_CERT_KEY=certs/client_key.pem

# Optional: Enable debug logging (true/false)
# DEBUG=false

# Optional: Custom timeout for Qlik connections (milliseconds)
# QLIK_TIMEOUT=30000

# ============================================================================
# SETUP INSTRUCTIONS:
# ============================================================================
# 
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Update QLIK_SERVER_URL with your Qlik Sense server hostname or IP
#
# 3. Obtain SSL certificates from your Qlik Sense administrator:
#    - root.pem (Root certificate)
#    - client.pem (Client certificate)  
#    - client_key.pem (Client private key)
#    
#    Place these files in the certs/ directory (create it if it doesn't exist)
#
# 4. If using a different user directory or service account, update:
#    - QLIK_USER_DIRECTORY (common: INTERNAL, DOMAIN_NAME)
#    - QLIK_USER_ID (common: sa_engine, service_account_name)
#
# 5. For non-standard Qlik installations, you may need to adjust:
#    - QLIK_SERVER_PORT (default: 4747 for Engine API)
#    - Certificate paths if stored elsewhere
#
# ============================================================================
# CERTIFICATE SETUP NOTES:
# ============================================================================
#
# The certificates required are the same ones used for Qlik Sense Engine API
# access. Contact your Qlik Sense administrator to obtain:
#
# - Root certificate (validates the server)
# - Client certificate and private key (authenticates this client)
#
# These are typically exported from the Qlik Management Console (QMC) under:
# Certificates > Export certificates
#
# Security Note: Never commit these certificate files to version control!
# They are automatically excluded by .gitignore